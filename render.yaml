services:
  - type: web
    name: Zeerostock-Assignment
    env: node
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: npm start
    env:
      - key: NODE_ENV
        value: production
